kind: CORS
apiVersion: api.ibm.com/v1
metadata:
  name: cors-echo-api
  namespace: DPv5c_Product_Order-3
  version: 1.0.0
  tags: []
spec:
  rules:
    - action: allow
      originList:
        - '*'
      allowCredentials: false
      exposeHeaders:
        predefined: false
        custom: ''
        backend: false
